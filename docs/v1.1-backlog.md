# Dose — post-audit backlog

Items surfaced by the pre-merge adversarial audit of the v1.0 redesign (branch `redesign`, audited vs
tag `v1.0-hardened`). None is a launch blocker — the audit found zero. The four HIGH/MEDIUM findings and
the >64-notification LOW were fixed before merge; what remains is captured here so it isn't lost.

## Pre-merge device checks (do before v1.0 ships — not code)
Can't be verified in a headless simulator; confirm on a real device with realistic data:
- [ ] **External-storage blob reclamation** — after deleting notes/photos, confirm the store's
      `_EXTERNAL_DATA` blob files are actually removed (no orphan accumulation). Row-level cascade is
      unit-tested (`testDeletingNoteCascadesToItsPhotos`); the on-disk file cleanup is OS-internal.
- [ ] **Real V5→V6 upgrade** of an actual shipped 5.0.0 store with real data. The bulk unit test
      (`testUpgradeFromLargeV5StorePreservesEveryRecord`) seeds a synthetic V5 store; a physical-device
      upgrade of a real store is the final proof.
- [ ] **DST delivery** — a snooze/reminder fires at the right wall-clock time across a spring-forward and
      a fall-back day (bucketing is unit-tested — `testDayBucketingIsDSTSafe`; OS delivery timing is device-only).
- [ ] **>64 notifications** — submit many concurrent snoozes; confirm no *today* reminder is evicted for a
      far-future one (the plan now caps at 64 — `testBudgetNeverExceedsCapEvenWhenSnoozesFillIt` — but the
      OS eviction order is device-observable only).
- [ ] **Live snooze fires exactly once**, at the snoozed time (unit tests prove the plan; delivery is device).

## v1.1 — Notes polish (MEDIUM)
- [ ] **Downsample photos on import** (~1600px, JPEG ~0.8) instead of storing original bytes; decode
      thumbnails off the main thread (`preparingThumbnail`/ImageIO). Today: full-res originals stored +
      decoded in the view body → disk bloat + scroll jank. (`NoteEditorView.loadPhotos`,
      `NoteComponents.PhotoAttachmentRow`.)
- [ ] **Archived-medicine picker mismatch** — the note editor's medicine picker lists only
      active-confirmed meds, so a note linked to an *archived* medicine shows "None" in the editor while
      the list row shows the name. Make both agree (include the linked archived med in the picker, or
      filter the list name through `Medicine.activeConfirmed`).
- [ ] **Corrupt-photo placeholder** — a corrupt/missing blob is counted ("1 photo") but not shown and
      can't be removed. Render a removable placeholder tile so the count matches and the broken row is
      deletable. (`NoteComponents.PhotoAttachmentRow`, the `UIImage(data:)` nil branch.)

## v1.1 — LOW / cleanup
- [ ] **Week "Snoozed" tile** counts raw snooze logs, so re-snoozing one slot counts twice; de-dup by slot.
- [ ] **Snooze past course-end** — a snooze on a course's last day can re-arm a reminder past the
      inclusive end date; bound the snooze re-arm by `scheduleEnd`. (`NotificationPlanner` snooze rebuild.)
- [ ] **Stale doc comment** — `NotificationPlan.snoozes` still says "≤10 min / imminent by construction",
      no longer true with variable snooze up to 180 min; that assumption underpins the snooze-first budget priority.

## Intentionally kept (documented decisions, not bugs)
- **Grace boundary (Option 2, 2026-07-09):** analytics/PDF apply NO grace — a past-due dose trends missed
  the moment its time passes (clinically honest, historically correct); Today shows an in-grace dose as
  overdue (red / "Overdue"), never neutral. Consistent in *meaning*, not wording. See the comment on
  `ExecutionEngine.defaultGrace`.
- **Streak keeps its 60-min grace** while adherence has none (pre-existing). Under Option 2 this is
  intentional (streak = "today-in-progress won't break it"; adherence = strict). Revisit only if a future
  decision unifies the two grace rules.
